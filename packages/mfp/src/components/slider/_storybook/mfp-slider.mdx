import { ArgTypes, Title, Subtitle } from '@storybook/addon-docs';

<div className="mfp-doc__wrapper" data-theme="light">
  <div className="mfp-doc__container" >
    <Title>Slider</Title>

    O componente **Slider** é um elemento de interface de usuário que permite aos usuários selecionar um valor de um intervalo de valores deslizando um "thumb" ao longo de uma trilha.
    É um componente amplamente utilizado em interfaces de usuário, comumente usado para ajustar configurações, inserir valores numéricos ou filtrar conteúdo.

    <Subtitle>Uso</Subtitle>

    Ao projetar uma interface de usuário, é importante usar componentes **Slider** de forma intuitiva e que melhorem a experiência do usuário. Aqui estão algumas melhores práticas para o uso de componentes **Slider**:

    - Use um **Slider** quando houver necessidade de ajustar valores dentro de um intervalo específico. **Sliders** são uma ótima escolha quando você deseja que os usuários possam ajustar algo, mas dentro de um intervalo predeterminado.
    - Use um **Slider** quando o intervalo de valores for contínuo. **Sliders** são mais adequados para intervalos contínuos de dados, em vez de valores discretos.
    - Use um **Slider** quando o intervalo de valores for pequeno e fácil de compreender. **Sliders** são mais eficazes quando o intervalo de valores é pequeno o suficiente para ser facilmente entendido pelo usuário.
    - Use um **Slider** quando houver necessidade de fornecer feedback visual ao usuário. **Sliders** oferecem feedback visual que pode ser muito útil, especialmente quando usados em combinação com outros elementos de UI, como gráficos ou tabelas.

    <Subtitle>👍 Quando usar</Subtitle>

    **Sliders** são um elemento de interface de usuário versátil que pode ser usado em diversas situações. Aqui estão alguns cenários em que o uso de um **Slider** é uma boa escolha:

    - Quando houver necessidade de ajustar configurações ou inserir valores numéricos. **Sliders** são uma ótima maneira de permitir que os usuários ajustem configurações como volume, brilho ou tamanho da fonte.
    - Quando o intervalo de valores for contínuo e fácil de compreender. **Sliders** são mais adequados para intervalos contínuos de dados, em vez de valores discretos.
    - Quando houver necessidade de fornecer feedback visual ao usuário. **Sliders** oferecem feedback visual que pode ser muito útil, especialmente quando usados em combinação com outros elementos de UI, como gráficos ou tabelas.

    <Title>Propriedades</Title>

    <ArgTypes of="mfp-slider" />
  </div>
</div>
