import { ArgTypes, Title, Subtitle } from '@storybook/addon-docs';

<div className="mfp-doc__wrapper" data-theme="light">
  <div className="mfp-doc__container" >
    <Title>Spinner</Title>

    O componente **Spinner** é um elemento de interface de usuário usado para indicar que uma aplicação está carregando ou processando dados.
    Spinners são comumente usados para fornecer feedback ao usuário quando uma ação ou solicitação está sendo processada.
    Isso permite que o usuário saiba que a aplicação ainda está funcionando e que ele deve aguardar o resultado.

    <Subtitle>Uso</Subtitle>

    - Use o componente **Spinner** para indicar ao usuário que algo está acontecendo nos bastidores, como um processo demorado ou recuperação de dados.
    - Use o componente **Spinner** quando o usuário iniciar uma ação que levará algum tempo para ser concluída, como o envio de um formulário ou a recuperação de grandes quantidades de dados.
    - Use o componente **Spinner** com moderação e apenas quando necessário. O uso excessivo do componente **Spinner** pode ser distrativo e frustrante para o usuário.
    - Use uma mensagem clara e concisa para acompanhar o componente **Spinner**, como "Carregando" ou "Aguarde".

    <Subtitle>👍 Quando usar</Subtitle>

    - Quando o usuário inicia uma ação que levará algum tempo para ser concluída.
    - Quando os dados estão sendo recuperados ou processados em segundo plano.
    - Quando há um atraso no tempo de resposta da aplicação.

    <Title>Propriedades</Title>

    <ArgTypes of="mfp-spinner" />
  </div>
</div>
